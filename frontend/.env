# Production Configuration for SNSU-NotificationSystem
# NOTE: Netlify only hosts static frontend. Backend must be hosted separately (Railway, Render, Heroku, etc.)
# Update VITE_API_URL and VITE_SOCKET_URL with your actual backend URL after deploying it

VITE_API_URL=https://YOUR-BACKEND-URL/api
VITE_SOCKET_URL=https://YOUR-BACKEND-URL

# Local Development (uncomment for local dev)
# VITE_API_URL=http://localhost:5000/api
# VITE_SOCKET_URL=http://localhost:5000
